# Image_Recognition_System_Model
This project demonstrates an image recognition system built using a pre-trained MobileNetV2 model from TensorFlow/Keras. The model classifies objects in images uploaded by users through a Google Colab environment. Users can upload an image, and the system will return the top 3 predicted labels with their confidence scores.
